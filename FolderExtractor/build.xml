<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project default="build" name="FileExtractor">
	<property name="main.src.dir" value="markmann.dennis.fileExtractor.logic.StartUp" />
	<property name="build.dir" value="build/" />
	<property name="lib.dir" value="${basedir}/lib" />
	<property name="class.dir" value="${basedir}/classes" />
	
	<target name="build" description="Create a runnable jar file for the project and copies needed ressources.">
		<mkdir dir="build" />
		<jar destfile="build/FileExtractor.jar" filesetmanifest="mergewithoutmain">
			<manifest>
				<attribute name="Main-Class" value="markmann.dennis.fileExtractor.logic.StartUp" />
				<attribute name="Class-Path" value="." />
			</manifest>
			<fileset dir="./target/classes">
				<exclude name="*FileExtractor.jar" />
				<exclude name="*.zip" />
				<exclude name="*.log" />
				<exclude name="*.xml" />
			</fileset>
			<zipfileset excludes="META-INF/*.SF" src="./lib/log4j-1.2.17.jar" />
			<zipfileset excludes="META-INF/*.SF" src="./lib/MyLibraries.jar" />
		</jar>
		<copy todir="build/Settings">
			<fileset dir="./Settings" />
		</copy>
		<copy todir="build/Icons">
			<fileset dir="./Icons" />
		</copy>
	</target>
	
	<target name="zip" description="Creates a zip file including the build files.">
		<zip destfile="./build/release.zip">
			<fileset dir="build">
				<exclude name="*.zip" />
			</fileset>
		</zip>
	</target>

	<target name="clean build" description="Deletes the created build files for the project.">
		<delete>
			<fileset dir="build" includes="**/*" />
		</delete>
	</target>
	
	<target name="compile">
			<mkdir dir="${build.dir}" />
			<javac srcdir="${main.src.dir}" destdir="${build.dir}" includeantruntime="false" />
	</target>

</project>
